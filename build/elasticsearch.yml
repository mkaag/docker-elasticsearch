path:
  data: /data/data
  logs: /data/log
  plugins: /data/plugins
  work: /data/work

transport.tcp.compress: true

http:
  max_content_length: 100mb
  enabled: true
  jsonp.enable: false
  cors.allow-origin: "/.*/"
  cors.enabled: true

script.disable_dynamic: true